fix: 改进积分解析、重试策略与敏感信息脱敏并补充测试

涉及文件: checkin.py, tests/test_checkin.py(新增), README.md, .github/workflows/glados.yml, .gitignore(新增)

【正确性修复】
- H1 从签到 message 正则解析获得积分(parse_earned_points)，不再读取不存在的 points 字段
- M1 新增 require_json，非 JSON 响应抛异常进入重试
- M2 新增 is_retryable，仅 5xx/网络异常重试，4xx(过期 Cookie)直接失败

【隐私/健壮】
- M3 COOKIE_MIN_LENGTH 20→24，修复短 Cookie 脱敏前后片段重叠
- M4 推送失败响应截断至 200 字符后再记录

【可测性/结构/流程】
- M5 新增 tests/test_checkin.py(39 用例) 与 CI 测试步骤
- L1 删除 HEADERS_BASE 冗余 content-type
- L2 多账号 Cookie 支持 & 或换行分隔
- L3 push_all 改为数据驱动 PUSH_CHANNELS
- L4 main 在无 Cookie/全部失败/推送全失败时退出码 1
- L5 REPEAT_KEYWORDS 提升为模块常量
- L6 加签缺失时打印告警
- L7 pip install --no-cache-dir
- L8 keepalive 独立并发组
